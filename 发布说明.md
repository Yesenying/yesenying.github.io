# 叶森个人站：双平台发布

现状：两页成品在 dist/；已准备可复用构建和 GitHub Pages 工作流。尚未登录账号、创建远程仓库或部署。

## 内容来源

dist/ 是本站手写源文件，不要删除或忽略。运行 `node scripts/build.mjs` 生成可发布的 build/。没有第三方构建依赖。当前 .openai/hosting.json 是早期预览配置；本次按用户要求改用 GitHub Pages 与 Cloudflare Pages，不需要创建 Sites 托管项目。

## GitHub Pages

1. 把 personal-site/ 作为仓库根目录上传到你自己的 GitHub 仓库，默认分支 main。只上传这个网站目录，不上传鼠标工作区或本地技能库。
2. 仓库 Settings → Pages → Source 选择 GitHub Actions。
3. 运行 Publish Ye Sen to GitHub Pages 工作流。生成路径会自动适配用户站点或带仓库名的项目站点。
4. 工作流输出的 page_url 才是实际网址，不预先假定用户名和地址。

免费方案使用公开仓库；发布前应知道网站源代码和主题下载文件也会公开。

## Cloudflare Pages

推荐在 Workers & Pages 中创建 Pages 项目并连接上面同一个 GitHub 仓库。仓库授权只选择本站仓库即可。

- 生产分支：main
- 框架预设：None
- 构建命令：node scripts/build.mjs
- 输出目录：build
- 根目录：仓库根目录
- 环境变量：NODE_VERSION=22；不设置 BASE_PATH

这样同一次内容更新可以触发两个平台部署。若不希望连接仓库，也可以把本地生成的 build/ 通过 Pages Direct Upload 上传；但更新需要另外上传。

## 主站网址与搜索

待 Cloudflare 实际生产网址确认后，在 Cloudflare 构建变量和 GitHub 仓库 Actions Variables 中设置相同的 CANONICAL_ORIGIN（HTTPS 主站根网址，不带路径）。重新构建后两边每页的 canonical 都指向主站，并生成主站 sitemap。主站选择以后可以改成自己的域名。

canonical 是给搜索引擎的建议，不保证选择结果或搜索排名。尚未拿到主站地址时构建不会写入虚构的网址。

## 上线后验证

在两个真实生产地址检查首页、作品页、图片、页面跳转和主题下载；GitHub 项目路径下同样检查。确认 canonical 指向正确主站，sitemap 没有 localhost。完成前不声称已上线。
